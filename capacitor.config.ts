import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fj.com.myaccount.myfnpfNew',
  appName: 'myFNPF-New',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
